{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}



    <h1>Register</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.firstname) }}{#DM_ ajout manuel#}
        {{ form_row(registrationForm.lastname) }}{#DM_ ajout manuel#}
        {{ form_row(registrationForm.login) }}{#DM_ ajout manuel#}

        {# DM_ MANIP REPEATED PASSWORD #}
        {#{{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }} DM_ on enlève l'ancien plainPassword pour mettre password#}
        {# copié collé de la Doc synfony https://symfony.com/doc/current/reference/forms/types/repeated.html #}
        {{ form_row(registrationForm.password.first) }}{# DM_ ajout remplace le 'form' de la doc par 'registrationForm' de notre projet #}
        {{ form_row(registrationForm.password.second) }}{# DM_ ajout #}

        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn">Register</button>
    {{ form_end(registrationForm) }}
{% endblock %}
